{
  "_comment": "Push-notification wiring. Set \"api\" to the URL of your deployed Cloudflare Worker (see PUSH-NOTIFICATIONS.md). While it is empty the 'Get alerts' button stays hidden and the site behaves exactly as before. Example: \"https://mena-push.YOUR-SUBDOMAIN.workers.dev\"",
  "api": ""
}
