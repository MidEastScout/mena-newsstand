# --- Posting agent: personal data, NEVER commit ---
# WhatsApp exports contain private messages from group members.
agent/data/
*.whatsapp.txt
WhatsApp Chat*

# Secrets
.env
*.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node (only pulled in locally for browser-based verification — never deployed)
node_modules/
package.json
package-lock.json

# Trends intermediate: the broad per-outlet coverage sample is produced by
# fetch_headlines.py and consumed by build_trends.py within the same CI run.
# It's not browser-facing (trends.json is), so it isn't committed or deployed.
coverage.json
