feedparser==6.0.11
requests==2.32.3
google-genai
# Optional: only used by build_topstories.py when ANTHROPIC_API_KEY is set
# (semantic Top-5 clustering). The builder imports it lazily and falls back to a
# no-key heuristic, so an install/network hiccup never breaks the refresh.
anthropic
Pillow==12.2.0
pypdfium2==4.30.0
# Web-push sender for the hourly "top stories" notifications (scripts/send_push.py).
# Optional: if unset secrets leave push unconfigured the script exits 0, so this
# install never blocks a refresh. Pulls in `cryptography` for VAPID signing.
pywebpush==2.3.0
